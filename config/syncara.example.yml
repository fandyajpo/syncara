# Minimal Syncara configuration
# Single listener, single upstream, no health checks, no TLS.

listeners:
  - port: 8080

routes:
  - path: /
    proxy: http://localhost:3000

admin:
  port: 9090
  management:
    username: admin
    password: syncara123
